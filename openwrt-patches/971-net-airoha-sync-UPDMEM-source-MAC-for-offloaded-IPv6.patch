From: Gilly1970 <gilroyscott1970@gmail.com>
Subject: [PATCH] net: airoha: sync UPDMEM source MAC for offloaded IPv6 flows

IPv6 flow entries have no room for an inline source MAC: unlike IPv4, which
stores the full address in the entry, they only carry a 5-bit smac_id and the
hardware builds the L2 header from UPDMEM[smac_id] at transmit time.

Fix it by keeping the slot in sync with the source MAC the offloaded flow is
actually going to use, which the flow dissector already provides and which
airoha_ppe_flow_offload_replace() has already validated. Cache the programmed
value per port so the register write only happens when it changes.

Known limitation: flows on one port alternating between two different source
MACs (e.g. routed IPv6 on both the untagged device and a cloned VLAN upper
simultaneously) still share the single per-port slot - last writer wins and
the other flow egresses with a wrong source MAC. Detect the A->B->A pattern
and emit a ratelimited warning naming both MACs, so the condition is
diagnosable; the complete fix is a slot allocator over the 16
UPDMEM entries.
---
--- a/drivers/net/ethernet/airoha/airoha_eth.h
+++ b/drivers/net/ethernet/airoha/airoha_eth.h
@@ -605,6 +605,12 @@
 	int id;
 	int users;
 
+	/* source MAC currently programmed in UPDMEM[id], and the value it
+	 * last replaced (for slot-contention detection)
+	 */
+	u8 smac[ETH_ALEN];
+	u8 smac_prev[ETH_ALEN];
+
 	/* protect concurrent hw_stats and frag register accesses */
 	spinlock_t lock;
 
--- a/drivers/net/ethernet/airoha/airoha_ppe.c
+++ b/drivers/net/ethernet/airoha/airoha_ppe.c
@@ -339,6 +339,36 @@
 	br->src_mac_lo = get_unaligned_be32(eh->h_source + 2);
 }
 
+/* IPv6 flow entries have no room for an inline source MAC: the hw builds the
+ * L2 header from UPDMEM[smac_id] instead. That table is only refreshed when the
+ * MAC of the airoha net_device itself is set, so a stacked device carrying a
+ * different MAC (e.g. a VLAN upper with a cloned address) would make the hw
+ * transmit IPv6 with a stale source MAC. Keep the slot in sync with the address
+ * the offloaded flow actually egresses with.
+ */
+static void airoha_ppe_sync_upd_mem(struct airoha_gdm_dev *dev, const u8 *addr)
+{
+	struct airoha_gdm_port *port = dev->port;
+
+	if (ether_addr_equal(port->smac, addr))
+		return;
+
+	/* Flows on this port are alternating between two different source
+	 * MACs (e.g. routed IPv6 on both the untagged device and a VLAN
+	 * upper carrying a cloned address). They share the single UPDMEM
+	 * slot, so whichever flow was programmed last wins and packets of
+	 * the other egress with a wrong source MAC. Fixing this needs a
+	 * slot per distinct MAC (UPDMEM has 16 slots, SMAC_ID is 5 bits).
+	 */
+	if (ether_addr_equal(port->smac_prev, addr))
+		dev_warn_ratelimited(dev->eth->dev,
+				     "UPDMEM source-MAC slot contention on port %d (%pM <-> %pM), IPv6 offload will use a wrong source MAC for one of the flows\n",
+				     port->id, port->smac, addr);
+
+	ether_addr_copy(port->smac_prev, port->smac);
+	airoha_ppe_init_upd_mem(dev, addr);
+}
+
 static int airoha_ppe_foe_entry_prepare(struct airoha_eth *eth,
 					struct airoha_foe_entry *hwe,
 					struct net_device *netdev, int type,
@@ -416,6 +446,9 @@
 						  dsa_port);
 
 			smac_id = port->id;
+			if (type >= PPE_PKT_TYPE_IPV6_ROUTE_3T)
+				airoha_ppe_sync_upd_mem(dev,
+							data->eth.h_source);
 		}
 	}
 
@@ -1545,6 +1578,8 @@
 		     FIELD_PREP(PPE_UPDMEM_ADDR_MASK, port->id) |
 		     FIELD_PREP(PPE_UPDMEM_OFFSET_MASK, 1) |
 		     PPE_UPDMEM_WR_MASK | PPE_UPDMEM_REQ_MASK);
+
+	ether_addr_copy(port->smac, addr);
 }
 
 struct airoha_ppe_dev *airoha_ppe_get_dev(struct device *dev)
