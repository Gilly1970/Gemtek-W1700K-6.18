Index: mt76-2026.07.24~50480826/mt7996/main.c
===================================================================
--- mt76-2026.07.24~50480826.orig/mt7996/main.c
+++ mt76-2026.07.24~50480826/mt7996/main.c
@@ -1707,10 +1707,17 @@ mt7996_ampdu_action(struct ieee80211_hw
 				   ssn, params->buf_size);
 		ret = mt7996_mcu_add_rx_ba(dev, params, vif, true);
 		break;
-	case IEEE80211_AMPDU_RX_STOP:
+	case IEEE80211_AMPDU_RX_STOP: {
+		struct mt76_rx_tid *rx_tid = rcu_dereference(msta->deflink.wcid.aggr[tid]);
+		u16 session_id = rx_tid ? rx_tid->id : 0;
+
 		mt76_rx_aggr_stop(&dev->mt76, &msta->deflink.wcid, tid);
 		ret = mt7996_mcu_add_rx_ba(dev, params, vif, false);
+
+		if (session_id && mt7996_has_hwrro(dev))
+			mt7996_mcu_wed_rro_reset_sessions(dev, session_id);
 		break;
+	}
 	case IEEE80211_AMPDU_TX_OPERATIONAL:
 		mtxq->aggr = true;
 		mtxq->send_bar = false;
